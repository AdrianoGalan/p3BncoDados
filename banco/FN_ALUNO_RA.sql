USE SIGA
GO

CREATE FUNCTION FN_ALUNO_RA(@RA INT)
RETURNS @table TABLE (
RA_ALUNO		INT,
NOME            varchar(MAX)
)
AS
BEGIN

    INSERT INTO @table(RA_ALUNO,NOME)
    SELECT A.RA, A.NOME FROM ALUNO A
    WHERE A.RA = @RA

	RETURN
END
